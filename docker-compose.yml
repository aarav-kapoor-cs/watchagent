services:
  watchagent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Named volume keeps the SQLite DB across `docker compose down`/`up`.
      - watchagent-data:/data
    restart: unless-stopped

volumes:
  watchagent-data:
